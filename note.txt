	//  Plugin version
		// Plugin slug
			// Holds various class instances.
            	// define constructor
// deifne singletone design pattern
	//  Magic getter to bypass referencing plugin.
	// define constants 
	// init plugin Load the plugin after all plugins are loaded.
	// activated methods 
	// deactivated methods 
	// install plugin 
	// includes function 
	// init hooks
	// init_classes
	// localization_setup
	// is_request What type of request is this.
	// plugin_action_links  Plugin action links 
	// Initialize the main plugin.
	// Kick-off the plugin.


necessary dependency 
=======================

add typescript:
npm install typescript @types/react @types/react-dom --save-dev
npx tsc --init


npm install @wordpress/element @types/react @types/react-dom --save
npm install @wordpress/element

npm install react-router-dom
npm install @types/react-router-dom --save-dev